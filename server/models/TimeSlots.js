// models/TimeSlot.js
const mongoose = require('mongoose');

const timeSlotSchema = new mongoose.Schema({
  // Define time slot schematic
});

module.exports = mongoose.model('TimeSlot', timeSlotSchema);
